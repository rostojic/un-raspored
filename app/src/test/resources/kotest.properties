# Kotest configuration read from the test classpath.
# Default number of property-test iterations (>= 100) for the whole project,
# so property-based tests (design Properties 1-10) exercise at least 100 cases
# unless a test overrides it explicitly.
kotest.property.default.iteration.count=100
